# HageStack website

Next.js 14 (App Router). Clone, run `npm ci`, then `npm run dev`.

**Vercel:** leave **Root Directory** empty (repository root). Set `NEXT_PUBLIC_SITE_URL` to your production URL.

Optional Base44: set the `NEXT_PUBLIC_*` variables from the Base44 dashboard if you use that integration.
